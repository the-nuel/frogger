{
  "name": "@nuel/frogger",
  "version": "1.0.1",
  "files": [
    "lib"
  ],
  "description": "Chains together a sequence of async tasks.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prebuild": "rm -rf lib/",
    "build": "uniformly build",
    "postbuild": "npm run generate-types",
    "test": "uniformly test",
    "generate-types": "tsc -d --declarationDir lib --emitDeclarationOnly",
    "prepare": "npm run build"
  },
  "author": "Tom Davies <tom@thenuel.com>",
  "license": "MIT",
  "repository": "github:the-nuel/frogger",
  "devDependencies": {
    "@nuel/uniformly": "^2.0.0"
  },
  "dependencies": {
    "@types/node": "^12.6.8"
  }
}
